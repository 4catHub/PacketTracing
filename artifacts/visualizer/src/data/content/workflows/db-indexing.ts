import { ContentItem } from "../content-types";

export const dbIndexingContent: ContentItem = {
  slug: "db-indexing",
  category: "workflow",
  title: "데이터베이스 인덱싱 처리 과정",
  subtitle: "B+ Tree 인덱스를 활용한 데이터 탐색 및 삽입 성능 비교 시각화",
  tags: [
    "Database",
    "Indexing",
    "B+ Tree",
    "Performance",
    "Query Optimization"
  ],
  description: `데이터베이스 인덱싱은 대규모 테이블에서 조건에 맞는 데이터를 고속으로 조회하기 위해 별도의 정렬된 자료구조를 유지 관리하는 기법입니다. 다음은 인덱싱 처리 과정의 핵심 작동 메커니즘을 7가지 질문과 답변 형식으로 상세히 설명합니다.

### Q1. 왜 Full Scan은 느린가?
인덱스가 구축되지 않은 테이블에서 특정 레코드를 조회할 때 데이터베이스 엔진은 테이블 전체의 블록을 맨 처음부터 무작위로 다 훑어 내려가는 테이블 스캔(Table Scan) 혹은 풀 스캔(Full Scan) 방식을 수행합니다. 이 방식은 메모리나 디스크에 적재된 모든 물리 페이지를 순차적으로 전수 검사해야 하므로 대량의 임의 디스크 입출력(I/O)이 유발됩니다. 최악의 경우 찾는 데이터가 물리 디스크 파일의 맨 마지막 블록에 들어있을 경우 모든 공간을 훑기 때문에 데이터 검색 시간이 전체 테이블 행 수에 비례하여 선형적으로(O(N)) 증가하게 되며 시스템 전반의 성능 저하를 초래합니다.

### Q2. 인덱스는 어떻게 만들어지는가?
인덱스를 정의하면 데이터베이스 엔진은 지정된 컬럼의 키 값들과 해당 레코드가 디스크 상에 저장된 실제 물리적 위치 정보(포인터 혹은 ROWID)를 추출합니다. 그리고 이 키 값들을 정렬(Sorting)한 뒤, 고속 탐색이 가능하도록 다방 구조의 균형잡힌 수형도인 B+ Tree(Balanced Tree) 자료구조를 구성합니다. 이 구조는 크게 루트 노드, 브랜치 노드, 리프 노드로 분류됩니다. 실제 디스크 상의 데이터 레코드 포인터들은 가장 하단에 놓인 리프 노드에만 물리적으로 정렬되어 삽입되며, 상위 계층 노드들은 최적의 탐색 분기점(이정표) 역할만을 담당하도록 설계됩니다.

### Q3. 데이터가 추가되면 인덱스는 어떻게 갱신되는가?
테이블에 새로운 레코드가 추가(INSERT)될 때, 인덱스 역시 정렬 상태를 보존하기 위해 실시간으로 업데이트되어야 합니다. 추가될 데이터의 키 값을 바탕으로 인덱스 트리를 하향 탐색하여 적절한 리프 노드를 식별한 후 삽입을 시도합니다. 이때 리프 노드의 수용 용량이 가득 차 있다면 노드를 반으로 쪼개고 중앙 키 값을 부모 브랜치 노드로 올려보내는 노드 분할(Node Split) 과정이 수행됩니다. 노드 분할이 연쇄적으로 일어나면 트리의 균형을 맞추기 위한 재조정(Rebalancing) 오버헤드가 발생하여 쓰기 작업 속도는 다소 느려지지만, 탐색 깊이가 치우치지 않게 자가 균형을 완벽히 보장합니다.

### Q4. SELECT가 실행되면 어떤 일이 일어나는가?
인덱스가 활성화된 컬럼을 검색 조건(WHERE 조건절)으로 활용해 SELECT 쿼리를 요청하면, 데이터베이스 내부의 쿼리 옵티마이저(Optimizer)가 비용 기반 분석을 실행합니다. 옵티마이저는 전체 디스크를 스캔하는 풀 스캔보다 인덱스를 거쳐 특정 타겟만 찾아내는 인덱스 탐색 비용이 현저히 저렴하다고 판단하면, 테이블 전체 탐색을 배제하고 해당 인덱스(idx_users_id)를 통한 포인트 룩업(Point Lookup) 실행 계획을 수립한 뒤 이를 스토리지 엔진에 전달합니다.

### Q5. B+ Tree는 어떻게 탐색되는가?
스토리지 엔진은 메모리 버퍼 풀에 올라와 있는 인덱스의 최상단 루트 노드(Root Node)부터 탐색을 시작합니다. 찾으려는 대상 키 값을 노드 내부의 정렬된 키들과 비교하여 분기 방향을 판단합니다. 검색 키가 현재 비교 대상 키보다 크거나 같으면 다음 우측 포인터를 타는 방식으로 탐색 경로를 좁히고 하위 브랜치 노드로 이동합니다. 이러한 비교 작업을 각 단계마다 거치며 아래로 점진 하강(Traversal)하여 최종적으로 키와 포인터가 실재하는 최하단의 리프 노드(Leaf Node)에 도달하게 됩니다.

### Q6. 실제 데이터는 어떻게 찾는가?
탐색을 거쳐 최종 리프 노드에 안착하면, 노드 내부의 키 배열을 검색하여 대상 키를 식별합니다. 리프 노드에는 단순 키 값만 있는 것이 아니라 해당 키를 품고 있는 데이터 파일 내의 실제 행 주소인 ROWID(물리적 페이지 번호 및 슬롯 오프셋)가 매핑되어 있습니다. 엔진은 이 포인터 정보를 건네받아 물리 디스크의 타겟 페이지를 찾아내고, 해당 주소로 다이렉트 점프(Bookmark Lookup 혹은 RID Lookup)하여 디스크로부터 실제 필요한 레코드를 메모리 버퍼 풀로 적재한 후 최종 조회 결과를 반환합니다.

### Q7. 왜 이것이 빠른가?
풀 스캔은 100만 개의 데이터 블록이 있으면 최악의 경우 100만 회의 디스크 블록 읽기 작업을 반복해야 합니다. 반면 B+ Tree 인덱스를 타게 되면, 트리의 높이가 극도로 낮게 유지되기 때문에(100만 행 기준 보통 높이 3~4 이내) 단 몇 번의 메모리 포인터 이동만으로 탐색을 마칩니다. 즉, 수천만 건의 데이터를 보유하고 있더라도 단 3~4회 수준의 비교와 마지막 단 1회의 디스크 물리 페이지 조회(O(log N))만으로 원하는 행을 고속 추출하므로 풀 스캔 대비 압도적인 성능적 이점을 취할 수 있습니다.`,
  steps: [
    "왜 Full Scan은 느린가? — 인덱스가 존재하지 않아 테이블 전체 레코드를 순차 검색하므로 디스크 I/O가 매 페이지마다 발생하여 조회 속도가 현저히 느려집니다.",
    "인덱스는 어떻게 만들어지는가? — 테이블의 특정 컬럼 키들을 모아 오름차순으로 정렬한 뒤, 계층식 B+ Tree 인덱스를 메모리 공간에 구축하여 최적의 경로를 배치합니다.",
    "데이터가 추가되면 인덱스는 어떻게 갱신되는가? — 레코드가 새로 추가될 때 인덱스 리프 노드 용량이 초과되면 노드를 분할하는 Node Split이 작동해 인덱스 구조를 실시간 재조정합니다.",
    "SELECT가 실행되면 어떤 일이 일어나는가? — 특정 키 값을 타겟으로 하는 SELECT 쿼리가 실행되면, Full Scan을 배제하고 인덱스 트리를 진입점으로 삼아 탐색에 돌입합니다.",
    "B+ Tree는 어떻게 탐색되는가? — 메모리 상의 루트 노드에서 키 값을 비교한 뒤 해당되는 서브 브랜치를 거쳐 최하위 리프 노드까지 점진적으로 하강해 나갑니다.",
    "실제 데이터는 어떻게 찾는가? — 도달한 단말 리프 노드의 ROWID 또는 물리 주소 값을 참조하여 디스크 상의 목표 블록 위치로 다이렉트 I/O 액세스를 수행합니다.",
    "왜 이것이 빠른가? — 테이블 전체 블록을 검색하는 대신 트리 높이에 수렴하는 수 차례의 메모리 탐색 및 단 1회의 디스크 Page 읽기로 조회 처리를 종결하기 때문입니다."
  ],
  examples: [
    "대량 테이블에서 특정 ID로 조회 시 디스크 블록 읽기 감소",
    "외래키 JOIN 시 인덱스를 경유하여 매칭 성능 향상",
    "ORDER BY 절에 인덱스를 적용하여 불필요한 별도 정렬 비용 제거",
    "B+ Tree 노드 분할 오버헤드를 절감하기 위한 클러스터형 인덱스 고려"
  ]
};
