import { ContentItem } from "../content-types";

export const jwtVsSessionContent: ContentItem = {
    slug: "jwt-vs-session",
    category: "workflow",
    title: "로그인 및 로그아웃 (Session vs JWT)",
    subtitle: "콘서트 입장권 비유로 이해하는 상태유지(Stateful) vs 무상태(Stateless) 인증 아키텍처",
    tags: [
        "Security",
        "Authentication",
        "JWT",
        "Session"
    ],
    description: `웹 애플리케이션의 인증과 인가 처리 방식은 서버 아키텍처와 확장성에 결정적인 영향을 미칩니다. 이 시각화에서는 콘서트 입장이라는 일상적인 비유를 통해, 서버 측에 로그인 상태를 저장하는 Stateful 세션 방식과 클라이언트 토큰 자체에 상태를 담는 Stateless JWT 방식의 동작 메커니즘을 상세히 대조합니다.

### 세션 기반 인증의 작동 원리 (매표소 장부와 단순 팔찌)
서버가 로그인 성공 시 세션 DB나 메모리 장부에 사용자 세션 객체를 기록하고, 클라이언트에게는 무작위 식별자인 Session ID 쿠키를 발급합니다.
- 로그인 처리: 서버의 중앙 집중형 세션 저장소에 로그인 상태를 직접 생성하므로, 세션 가로채기 방어를 위한 SameSite 및 HttpOnly 쿠키 설정이 권장됩니다.
- API 요청 검증: 클라이언트가 매 요청마다 팔찌에 적힌 Session ID를 보내면 게이트가 매표소 DB를 매번 룩업하여 세션 존재 유무를 교차 검증합니다.
- 로그아웃 처리: 서버의 세션 장부에서 해당 ID 레코드를 즉시 삭제하여, 해당 쿠키를 가진 요청을 즉각적이고 완벽하게 무효화합니다.

### JWT 토큰 인증의 작동 원리 (위조 불가능한 스마트 티켓)
서버가 로그인 성공 시 사용자의 신원 정보와 만료일 등을 담아 디지털 서명한 토큰 객체(JWT)를 발행하여 클라이언트에 넘겨줍니다. 서버에는 어떤 정보도 저장하지 않습니다.
- 로그인 처리: 토큰 자체에 모든 권한이 들어가므로 XSS 공격을 막기 위한 토큰 보관 위치 선정(예: 보안 수준이 높은 HttpOnly 쿠키)이 매우 중요합니다.
- API 요청 검증: 무인 게이트가 토큰의 서명 무결성만을 암호학적으로 자체 해석하여 판단하므로, 서버 간 데이터 정합성을 맞출 필요가 없어 대규모 서비스에서 뛰어난 수평 확장성을 보여줍니다.
- 로그아웃 처리: 이미 발행된 토큰은 서버가 강제로 뺏을 수 없어 만료 전까지는 유효합니다. 따라서 클라이언트 측에서의 토큰 삭제뿐만 아니라, 탈취 방지를 위해 Access Token의 유효 기간을 극도로 짧게 하고 Redis 등을 활용한 블랙리스트 시스템을 가동하는 별도의 무효화 아키텍처가 요구됩니다.

### 로그인 시 토큰 저장소 및 보안 설계
로그인 처리를 수행할 때 인증 수단을 클라이언트의 어디에 저장할지가 보안의 핵심 쟁점입니다.
- 로컬 스토리지 저장: 자바스크립트 코드로 쉽게 접근할 수 있어 편리하지만, 악성 스크립트 삽입을 통한 XSS 공격에 매우 취약하여 토큰이 고스란히 유출될 수 있습니다.
- HttpOnly 쿠키 저장: 자바스크립트로 접근할 수 없도록 브라우저 수준에서 차단하여 XSS 공격으로부터 토큰 유출을 방어합니다. 다만 이 경우 사이트 간 요청 위조(CSRF) 공격 경로가 열리므로 SameSite 속성을 Strict나 Lax로 설정하여 동일 출처 요청만 허용하도록 설계해야 합니다.

### 이중 토큰 전략 (Access & Refresh Token)
JWT의 무상태성을 보존하면서 보안을 강화하기 위해 상호 보완적인 두 종류의 토큰을 혼용하는 설계 전략이 널리 쓰입니다.
- Access Token: 실제 API 인가에 쓰이며, 탈취 사고를 대비하여 유효 기간을 매우 짧게(예: 15분에서 1시간) 설정합니다.
- Refresh Token: Access Token이 만료되었을 때 새로운 토큰을 발급받기 위한 용도로 쓰이며, 유효 기간을 길게(예: 2주) 설정하고 데이터베이스나 Redis에 저장하여 발급 이력을 서버에서 직접 제어 및 무효화할 수 있도록 보완합니다.

### 로그아웃 시 인증 무효화 한계 및 극복 전략
사용자가 명시적으로 로그아웃을 요청했을 때, 잔여 수명이 남은 토큰을 완전히 정지시키는 것이 보안 설계의 최종 과제입니다.
- Stateful 무효화: 세션 방식은 서버 저장소 내 해당 세션 키만 삭제하면 즉각 연결이 차단되므로 직관적이고 강력한 통제가 가능합니다.
- Stateless 무효화의 한계: JWT는 서버에 보관된 상태가 없으므로 클라이언트가 토큰을 버리더라도 만료 시점까지는 토큰 자체가 계속 유효합니다.
- 하이브리드 블랙리스트 극복: 이 한계를 극복하기 위해 로그아웃 요청 시 서버가 해당 토큰의 Signature 값을 추출하여 만료시각까지의 잔여 시간 동안 Redis의 블랙리스트 저장소에 올려둡니다. 이후 인가 필터에서 매번 블랙리스트를 조회하여 매치될 경우 접근을 조기에 차단하는 방어 전략을 취합니다.`,
    steps: [
        "대기 상태: 관람객(클라이언트)이 로그인을 위해 콘서트장 매표소에 접근합니다.",
        "로그인 요청: 매표소에 신분증(ID/PW)을 제출하여 신원 확인을 요청합니다.",
        "인증 완료 및 발급: 세션 방식은 장부에 등록 후 '단순 팔찌'를 발급하며, JWT 방식은 자체 암호화 서명이 포함된 '스마트 티켓'을 발급합니다.",
        "인가된 API 요청: 세션 게이트는 매번 서버 장부를 룩업해야 통과되고, JWT 게이트는 스마트 티켓을 자체 검증하여 딜레이 없이 통과시킵니다.",
        "로그아웃 및 무효화: 세션은 서버 장부의 기록을 지워 즉시 출입을 막습니다. JWT는 클라이언트가 티켓을 폐기하고, 서버는 위험 토큰을 블랙리스트 전광판에 등록하여 방어합니다."
    ],
    examples: [
        "단일 모놀리식 서버 기반 서비스 (세션 인증에 적합)",
        "대규모 다중 서버 및 MSA(마이크로서비스) 아키텍처 (JWT 인증에 적합)",
        "외부 API 및 서드파티 제휴 서비스 연동 (JWT/OAuth2에 적합)",
        "실시간 금융 거래 등 즉각적인 강제 로그아웃이 필수적인 서비스 (세션 인증 권장)"
    ],
    related: [
      { slug: "oauth-flow", category: "workflow", relation: "OAuth 2.0 인증 절차 완료 후 토큰 기반 인가 방식" },
      { slug: "api-gateway", category: "workflow", relation: "Gateway 진입점에서 JWT 토큰을 자체 파싱하여 인가 처리" },
      { slug: "monolith-vs-msa", category: "workflow", relation: "Monolith(세션 선호) vs MSA(Stateless JWT 선호) 아키텍처 선택" }
    ]
};

